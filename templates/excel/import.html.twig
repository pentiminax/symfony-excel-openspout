{% extends 'base.html.twig' %}

{% block title %}Import Excel{% endblock %}

{% block body %}
    {{ form(form) }}

    {% if data is not empty %}
        <table>
            {% for row in data %}
                <tr>
                    {% for cell in row %}
                        <td>{{ cell }}</td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </table>
    {% endif %}
{% endblock %}
